import { Route } from '@angular/router';
import { ActivateAccountComponent } from './activate-account.component';

export const activateAccountRoutes: Route[] = [
    {
        path     : '',
        component: ActivateAccountComponent
    }
];
